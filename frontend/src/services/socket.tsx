// const server = socket.connect();

// export const sendMessage = () => {
//   server.emit("clientMessage", "testando");
// };

// export const Message = () => {
//   socket.on("serverMessage", (data) => {
//     console.log(`Recebi a mensagem do server: ${data}`);
//     return data;
//   });
// };
